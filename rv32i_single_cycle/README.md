# RV32I Single-Cycle Processor

Starter project for a clean, modular RV32I single-cycle processor in Verilog.

## Structure

- `rtl/` : synthesizable RTL modules
- `tb/` : testbenches
- `mem/` : instruction/data memory initialization files
